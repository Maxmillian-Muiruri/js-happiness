// Working with objects and types
type Student = {
    id: number;
    name: string;
    course: string;
    active: boolean;
};

const student: Student = {
    id: 1,
    name: "Maxmillian",
    course: "Computer Science",
    active: true,
};

console.log("Student Details:", student);
